fixtures:
    symlinks:
      doubledog-mirrmaid: "#{source_dir}"
    repositories:
      ddolib:
        repo: https://github.com/jflorian/doubledog-ddolib.git
      stdlib:
        repo: https://github.com/puppetlabs/puppetlabs-stdlib.git
        ref: 4.25.1
    forge_modules:
        concat: puppetlabs-concat
